# 热床管理

点击工具栏的 **热床管理** 按钮，切换 3D 打印热床的显示与隐藏。

程序根据模型类型自动决定初始状态：

- **CAD 模型**（STL / STEP / 3MF 等）：默认 **显示热床**
- **其他模型**（GLB / OBJ / PLY 等）：默认 **隐藏热床**

当前加载的 `vise.3mf` 为 3MF 格式，热床默认显示。

![](/screenshots/toolbar/zh/bed-management-panel.png)

适合 3D 打印用户在切片前预览模型在热床上的摆放位置。
